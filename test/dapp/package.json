{
    "name": "@scom/scom-nostr-dapp-test",
    "version": "0.1.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "devDependencies": {
        "@ijstech/cli": "^0.1.28",
        "@ijstech/compiler": "^0.1.17",
        "@ijstech/components": "^0.1.29",
        "@ijstech/eth-contract": "^0.1.8",
        "@ijstech/eth-wallet": "^0.1.29",
        "@scom/dapp": "^0.1.5"
    },
    "scripts": {
        "build": "rm -rf dist && npx @ijstech/compiler",
        "serve": "npx @ijstech/cli serve 8080",
        "test": "npm run build && npm run serve"
    }
}